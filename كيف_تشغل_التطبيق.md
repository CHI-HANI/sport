# كيف تشغّل تطبيق FITPULSE PRO

## الطريقة 1: فتح الملف مباشرة (الأبسط)

1. افتح مجلد المشروع في **File Explorer** (مستكشف الملفات).
2. انقر نقراً مزدوجاً على الملف:
   - **`index_final.html`**
3. سيفتح التطبيق في المتصفح الافتراضي (Chrome أو Edge عادةً).

> **ملاحظة:** إذا كان عندك `features-complete.js` وتحميله من الملف قد لا يعمل أحياناً (سياسة الملفات المحلية). استخدم الطريقة 2 في هذه الحالة.

---

## الطريقة 2: عبر سيرفر محلي (موصى بها للـ PWA و AI)

### أ) من VS Code / Cursor

1. ثبّت الامتداد **Live Server** (لو لم يكن مثبتاً):
   - اضغط `Ctrl+Shift+X` لفتح الامتدادات.
   - ابحث عن **Live Server** وثبّته.
2. انقر يميناً على ملف **`index_final.html`**.
3. اختر **"Open with Live Server"**.
4. سيفتح التطبيق في المتصفح على عنوان مثل: `http://127.0.0.1:5500/index_final.html`

### ب) من الطرفية (Terminal)

**إذا عندك Node.js:**

```bash
cd "c:\Users\CHIHANI\Downloads\sport\‏‏sport v1 - نسخة"
npx --yes serve -l 5500
```

ثم افتح في المتصفح: **http://localhost:5500** واختر `index_final.html`.

**إذا عندك Python فقط:**

```bash
cd "c:\Users\CHIHANI\Downloads\sport\‏‏sport v1 - نسخة"
python -m http.server 5500
```

ثم افتح: **http://localhost:5500/index_final.html**

---

## الطريقة 3: من Cursor (زر التشغيل)

1. افتح المشروع في Cursor.
2. من القائمة: **Run → Start Debugging** (أو اضغط `F5`).
3. اختر الإعداد **"فتح FITPULSE (سيرفر محلي)"**.
4. تشغّل سيرفر محلي على المنفذ 5500 (مثلاً بـ Live Server أو أمر `npx serve` أعلاه)، ثم افتح الرابط في المتصفح.

---

## ملخص سريع

| الطريقة              | الخطوة الرئيسية                          |
|----------------------|-------------------------------------------|
| **ملف مباشر**        | نقر مزدوج على `index_final.html`         |
| **Live Server**      | يمين على الملف → Open with Live Server   |
| **Node (npx serve)** | `npx serve -l 5500` ثم فتح localhost:5500 |
| **Python**           | `python -m http.server 5500` ثم فتح الرابط |

بعد التشغيل ستظهر واجهة التطبيق ويمكنك التجربة. إذا كان لديك زر 🤖 (AI Coach) فتأكد أنك تشغّل عبر سيرفر محلي حتى يعمل تحميل `features-complete.js` وطلبات الـ API بدون مشاكل.
